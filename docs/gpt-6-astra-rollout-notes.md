# GPT-6 Astra rollout notes

OpenAI announced GPT-6 Astra on September 3, 2026. Initial access is rolling out rather than universally available on day one.

For 3DVR, availability and promotion are separate questions. We can merge compatibility before our account receives API access, then enable the Astra lane through configuration when access is confirmed.

Do not make Astra the global default just because the model becomes available. First use it as a frontier supervisor for difficult work, keep economical workers for routine tasks, and use eval receipts to decide where it earns the higher per-token cost.

Coordination note: this track should stay narrow while Digital Organism, Operator, release, and deployment work continue in parallel. Avoid unrelated refactors in Astra compatibility changes.
