# LicheePi on-site recovery window — 2026-09-04

Thomas is physically near the LicheePi for a limited window. This marker intentionally triggers the gated resilience drill after the boot rollback guard passes.

The known-good extlinux default must remain `l0`; experimental Debian mainline remains non-persistent until recovery behavior is proven.
