name: Digital Organism Real Evidence Tournament

on:
  push:
    branches: [main]
    paths: [".github/workflows/organism-real-tournament-once.yml"]

permissions:
  contents: read

jobs:
  evaluate:
    name: Evaluate live retrieval evidence
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      DO_AGENT_HOST: 167.172.193.194
      DO_AGENT_USER: root
      THREEDVR_NODE_BIN: /opt/node-v22/bin/node
    steps:
      - uses: actions/checkout@v4
      - name: Resolve DigitalOcean SSH key
        shell: bash
        env:
          SSH_A: ${{ secrets.THREEDVR_SSH_PRIVATE_KEY }}
          SSH_B: ${{ secrets.DO_SSH_PRIVATE_KEY }}
          SSH_C: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          key=''
          for candidate in "$SSH_A" "$SSH_B" "$SSH_C"; do
            if [ -z "$key" ] && [ -n "$candidate" ]; then key="$candidate"; fi
          done
          if [ -z "$key" ]; then echo 'No DigitalOcean SSH key is configured.' >&2; exit 1; fi
          umask 077
          printf '%s\n' "$key" > /tmp/3dvr-agent-key.raw
          if grep -q 'BEGIN .*PRIVATE KEY' /tmp/3dvr-agent-key.raw; then
            cp /tmp/3dvr-agent-key.raw /tmp/3dvr-agent-key
          elif base64 -d /tmp/3dvr-agent-key.raw > /tmp/3dvr-agent-key 2>/dev/null && grep -q 'BEGIN .*PRIVATE KEY' /tmp/3dvr-agent-key; then
            :
          else
            echo 'Configured DigitalOcean SSH key could not be decoded.' >&2
            rm -f /tmp/3dvr-agent-key.raw /tmp/3dvr-agent-key
            exit 1
          fi
          chmod 600 /tmp/3dvr-agent-key
          rm -f /tmp/3dvr-agent-key.raw
          printf '%s\n' '167.172.193.194 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFoepoGbXWiidk4axaBwsI1Y/GWVckiqJxGLkBpTV6Pc' > /tmp/3dvr-known-hosts
          chmod 600 /tmp/3dvr-known-hosts

      - name: Diagnose and run gated real-evidence tournament
        shell: bash
        run: |
          set -euo pipefail
          opts=(-i /tmp/3dvr-agent-key -o BatchMode=yes -o UserKnownHostsFile=/tmp/3dvr-known-hosts -o StrictHostKeyChecking=yes -o ConnectTimeout=10)
          ssh "${opts[@]}" "$DO_AGENT_USER@$DO_AGENT_HOST" "THREEDVR_NODE_BIN='$THREEDVR_NODE_BIN' bash -s" <<'REMOTE'
          set -euo pipefail
          export THREEDVR_CONFIG_FILE="$HOME/.3dvr/config/env"
          if [ -f "$THREEDVR_CONFIG_FILE" ]; then set -a; . "$THREEDVR_CONFIG_FILE"; set +a; fi
          cd "$HOME/.3dvr/portal/apps/agent"
          [ -x "$THREEDVR_NODE_BIN" ] || exit 1
          echo 'Context HQ session counts (aggregate only):'
          "$THREEDVR_NODE_BIN" - <<'NODE'
          const { listSessions } = require('./thomas-agent/node/context-hq');
          const aliases = [...new Set([
            process.env.THREEDVR_CONTEXT_HQ_OWNER_ALIAS || '3dvr.tech@gmail.com',
            '3dvr.tech@gmail.com',
            '3dvr-managed',
            'tmsteph',
          ].filter(Boolean))];
          (async () => {
            const counts = await Promise.all(aliases.map(async alias => [alias, (await listSessions({ ownerAlias: alias })).length]));
            for (const [alias, count] of counts) console.log(`${alias}: ${count}`);
            process.exit(0);
          })().catch(error => { console.error(error.message || error); process.exit(1); });
          NODE
          echo 'Run one explicit organism sync cycle:'
          "$THREEDVR_NODE_BIN" - <<'NODE'
          const { runSyncOnce } = require('./thomas-agent/node/organism-sync');
          runSyncOnce().then(report => {
            console.log(`[organism-sync] ok=${report.ok} owner=${report.contextOwnerAlias} imported=${report.imported || 0} skipped=${report.skipped || 0}`);
            process.exit(report.ok ? 0 : 1);
          }).catch(error => { console.error(error.message || error); process.exit(1); });
          NODE
          echo 'Live Digital Organism evidence summary after sync:'
          "$THREEDVR_NODE_BIN" thomas-agent/node/retrieval-lab.js evidence
          echo 'Live Digital Organism real-evidence tournament:'
          "$THREEDVR_NODE_BIN" thomas-agent/node/retrieval-lab.js real-tournament --promote
          REMOTE

      - name: Remove SSH key
        if: always()
        shell: bash
        run: rm -f /tmp/3dvr-agent-key /tmp/3dvr-agent-key.raw /tmp/3dvr-known-hosts
