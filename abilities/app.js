const registry=document.querySelector('#registry');const summary=document.querySelector('#summary');const search=document.querySelector('#search');const filters=document.querySelector('#filters');const empty=document.querySelector('#empty');let data=null;let active='all';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){if(!data)return;const q=search.value.trim().toLowerCase();const rows=data.capabilities.filter(c=>(active==='all'||c.status===active)&&(!q||[c.name,c.category,c.description,c.permission,c.access].join(' ').toLowerCase().includes(q)));registry.innerHTML=rows.map(c=>`<article class="ability"><div><h2>${esc(c.name)}</h2><div class="category">${esc(c.category)}</div></div><div><p>${esc(c.description)}</p><div class="meta"><span class="chip ${esc(c.status)}">${esc(data.statuses[c.status]||c.status)}</span><span class="chip">Permission: ${esc(c.permission)}</span><span class="chip">Via: ${esc(c.access)}</span><span class="chip">Verified: ${esc(c.verified)}</span></div></div></article>`).join('');empty.hidden=rows.length>0;}
fetch('abilities.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}).then(d=>{data=d;const count=s=>d.capabilities.filter(c=>c.status===s).length;summary.innerHTML=`<span>${count('working')} working</span><span>${count('partial')} partial</span><span>${count('planned')} planned</span><span>${d.capabilities.length} recorded</span><span>Updated ${esc(d.updated)}</span>`;render()}).catch(()=>{registry.innerHTML='<p>Registry data could not be loaded.</p>'});
search.addEventListener('input',render);filters.addEventListener('click',e=>{const b=e.target.closest('button[data-status]');if(!b)return;active=b.dataset.status;filters.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));render()});
